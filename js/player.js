class Player{
  constructor(symbol, color){
    this.symbol = symbol;
    this.color = color;
    this.name = null;
  }

  getSymbol(){
    return this.symbol;
  }

  getColor()
  {
    return this.color;
  }
}
